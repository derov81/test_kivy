

<Container>:

    spinner: spinner_id

    Spinner:
        id: spinner_id
        size_hint: None, None
        size: 100, 44
        pos_hint: {'right': 1, 'top': 1}
        text: '<select>'
        values: ('Home', 'Work', 'Other')
        on_text: root.on_spinner_select(self.text)






